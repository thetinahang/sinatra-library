Sinatra library application -- Keep track of books and borrowers! 

To run this app, use the bundle install command, run the rake db:migrate command, and then use the shotgun gem to test in the browser. 